<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.fosterw1.finalprojectcalculator" >

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".SteelActivity"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:label="Start Menu"
            android:name=".MenuActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".GoldCoinActivity"
            android:label="@string/goldcoins" >
        </activity>
        <activity
            android:name=".IronOreActivity"
            android:label="@string/ironore" >
        </activity>
    </application>

</manifest>
